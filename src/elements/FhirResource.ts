import { Resource } from '../base/Resource';

/**
 * FhirResource is an alias for the base Resource class.
 * This represents any FHIR resource in the system.
 */
export type FhirResource = Resource;

// Also export the Resource class directly for convenience
export { Resource };